# User flow

Landing → Google OAuth → onboarding → structured generation → draft builder →
merchant publish → stable public URL → behavior events → cold-start hypothesis →
A/B preview → experiment → deterministic results → merchant selection → publish →
rollback or next experiment.

The Phase 0–5 boundary ends after public SSR, visitor/session identity, validated
event ingestion, and the clearly labelled no-payment demo conversion.
